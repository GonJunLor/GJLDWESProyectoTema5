# Redirección a indice personalizado
directoryindex indexProyectoTema5.php

# Para que fuincione el metodo head('WWW-Authenticate: Basic realm="Contenido restringido"') en php
RewriteEngine On
RewriteCond %{HTTP:Authorization} ^(.*)
RewriteRule .* - [E=HTTP_AUTHORIZATION:%1]

# Redirección de errores a paginas personalizadas
ErrorDocument 404 /error/404.html
ErrorDocument 403 /error/403.html
ErrorDocument 500 /error/500.html

#Redirección http a https (cambiar ip de https)
#RewriteEngine On
#RewriteCond %{SERVER_PORT} 80
#RewriteRule ^(.*)$ https://10.199.8.153/$1 [R,L]

# Variables de entorno conexión BBDD en entorno explotación
SetEnv DB_DSN "mysql:host=localhost; dbname=DBGJLDWESProyectoTema5"
SetEnv DB_USERNAME "userGJLDWESProyectoTema5"
SetEnv DB_PASSWORD "5813Libro-Puro"